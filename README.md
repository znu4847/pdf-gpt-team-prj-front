# Environment
1. set virtual environment
    ```sh
    python -m venv .venv
    ```


2. install package
    ```sh
    source .venv/bin/activate
    pip install -r requirements.txt
    ```

3. run streamlit
    ```sh
    streamlit run App.py
    ```